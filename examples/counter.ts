// encoding UTF-8
// (C) 2015 Muthiah Annamalai
நிரல்பாகம் counter() {
       மாரி z = [1,2,3,4];
       மாரி sum = 0;
       ஆக( மாரி i =0; i < z.length; i = i + 1 ) {
	     sum = sum + z[i];
     }
     பின்கொடு sum;
  }
alert( counter() )
